<div>
  <h1>View Albums</h1>
  {{#each albums}}
    <div>
      <h2> <a href="/albums/{{../artistId}}/tracks/{{id}}">{{this.name}}</a> </h2>

      <p>Release Date:{{release_date}}</p>
      <img src="{{this.images.1.url}}" alt="{{this.name}}" />

    </div>
  {{/each}}
</div>